<ContentDialog
    x:Class="StegoPlusPlus.Views.CDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StegoPlusPlus.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Close">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,10,0,10">
            <TextBlock Text="Secret Text / Message" Style="{StaticResource TitleTextBlockStyle}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,10,0,10">
            <TextBox x:Name="txt_DataMessage" IsReadOnly="True"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,10,0,10">
            <ToggleSwitch x:Name="toggle_Data" Header="View Mode" OffContent="Encrypt" OnContent="Decrypt" IsOn="False" Toggled="Toggle_Data_Toggled"/>
        </StackPanel>
    </Grid>
</ContentDialog>
